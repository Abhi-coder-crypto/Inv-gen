## Packages
recharts | Dashboard analytics charts
framer-motion | Page transitions and sleek animations
date-fns | Date formatting
react-to-print | Handling print/PDF export
@radix-ui/react-dropdown-menu | For polished menus
@radix-ui/react-dialog | For modals
@radix-ui/react-tabs | For auth switching
@radix-ui/react-switch | For settings toggles

## Notes
Authentication uses /api/user to check session
Invoice items need frontend calculation for subtotal/total before submission
PDF generation handled via browser print dialog with print-specific CSS
